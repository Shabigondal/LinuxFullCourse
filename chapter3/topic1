Permissions Chapter Started

Superuserdo  short form the sudo

The most powerful and used commands in linux

go to ubuntu type whoami

then go and type sudo whoami

In linux macos and unix like system directories are built in such a way that divided into 3 groups like One is owner rest of the two are other groups

-rwxr-xr--
drwxr-xr--


D means the directory r means read w means write x means execute so these are the three group and there permsissions accordingly like owner can read write and executre whereas
the second group can only read and execute similary 3rd group can only read 

to change the permissions of a direcotry here the step to follow


to run a executable file like .sh in a directory run it in the following way

directory/msd.sh

or if you are in same directory

./msd.sh

so now if you want to change the permissions here you go 

chmod -x msdh.sh

now file will not be executed

now if you want to again change permissions here you go 

chmod u+x msdh.sh

how to change ownership of the file here's the way to do so

chown -R username direcotry


chmod manages the permissions like read write and execute 
chown manages the ownership of the directory
